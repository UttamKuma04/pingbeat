[build]
builder = "RAILPACK"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && celery -A config worker --loglevel=info -P threads -c 8"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
