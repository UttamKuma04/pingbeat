[build]
builder = "RAILPACK"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && celery -A config beat --loglevel=info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
